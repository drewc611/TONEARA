{
  "name": "toneara",
  "version": "0.1.0-beta.1",
  "private": true,
  "description": "A browser-first AI-assisted music studio for generating, remixing, and exporting original instrumentals.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/validate.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test && npm run build"
  },
  "engines": { "node": ">=20" },
  "license": "SEE LICENSE IN LICENSE"
}
